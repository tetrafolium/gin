---
inspecode:

  global:

  tools:

    fortify:
      options:
        --commands:
          - apt-get install go
          - make GO="sourceanalyzer -b build_id go" test
        --work-dir: /
      thresholds: {num-issues: 999}
