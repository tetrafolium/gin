---
inspecode:

  global:

  tools:

    fortify:
    - thresholds: {num-issues: 999}
    - thresholds: {num-issues: 999}
      options:
        --commands:
          - make GO="sourceanalyzer -b build_id go"
        --work-dir: /
    - thresholds: {num-issues: 999}
      options:
        --commands:
          - make GO="sourceanalyzer -b build_id go" test
        --work-dir: /
